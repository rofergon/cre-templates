# Legacy Docs

This folder keeps project documentation that no longer matches the current protocol implementation.

Contents:

- `Workflow-CRE-Chainlink.legacy.md`
  - describes the old vesting/grants workflow
  - references `EmployeeVesting`
  - references `SYNC_CREATE_GRANT`, `GrantCreated`, `GrantRevoked`, and `TokensClaimed`
  - uses outdated trigger indexing

These files are preserved for review and cleanup decisions, but should not be used as source of truth for the current repo state.
